server:
  port: 8080

spring:
  application:
    name: api-gateway
  # All other config comes from config-server (config-repo/api-gateway.yml)
  config:
    import: optional:configserver:http://localhost:8888

jwt:
  secret: ${JWT_SECRET:12345678901234hjkouyqwtyeuahdbekw}
