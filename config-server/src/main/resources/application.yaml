server:
  port: 8888

spring:
  profiles:
    active: native
  application:
    name: config-server

  cloud:
    config:
      server:
        native:
          search-locations: file:///F:/hhcc-microservices-2.0/config-repo
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus